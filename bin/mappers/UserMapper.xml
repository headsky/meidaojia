<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.hs.mappers.UserMapper">
  <select id="getUser" resultType="com.hs.model.User">
    SELECT
     *
    FROM users
  </select>
  
    <insert id="insertUser">
    insert into user(userid) values('meidaojia1')
  </insert>
  
   <insert id="updateUser">
    update user set old=#{old}
  </insert>
</mapper>